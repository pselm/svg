{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "gulp make",
    "clean": "rimraf output && rimraf .pulp-cache && rimraf build",
    "test": "sh build-examples.sh"
  },
  "devDependencies": {
    "bower": "^1.8.4",
    "pulp": "^12.0.0",
    "purescript": "^0.11.7",
    "purescript-psa": "^0.6.0",
    "rimraf": "^2.6.2"
  }
}
